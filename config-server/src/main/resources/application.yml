server:
  port: 7070 

spring:
  application:
    name: config-server 

  cloud:
    config:
      server:
        git:
            uri: https://github.com/AlessandroPastor/DAD_2023_2-main.git
            search-paths: config-data
            #username:
            #password:
            default-label: main
  security:
    user:
      name: root
      password: 123456
